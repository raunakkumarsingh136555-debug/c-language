#include <stdio.h>
#include <string.h>

int main() {
    char a[50], b[50];
    scanf("%s %s", a, b);

    if(strcmp(a, b) == 0)
        printf("Same");
    else
        printf("Not Same");

    return 0;
}